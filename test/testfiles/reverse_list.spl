reverse ( list ) {
    var accu = [];
    while ( ! isEmpty ( list ) ) {
        accu = list.hd : accu ;
        list = list.tl;
    }
    return accu ;
}

main() {
    var l = [1, 2, 3];
    print(reverse(l));
}