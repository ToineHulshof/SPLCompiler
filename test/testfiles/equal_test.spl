f ( list ) {
    var accu = [];
    while ( ! isEmpty ( list ) ) {
        accu = list.hd : accu ;
        list = list.tl;
    }
    return accu ;
}

K(a, b){
    print(3);
    return a;
}

main() {
    print('h':'o':'i':[]);
    print(f(K([1, 2, 3], 'h':[])));
}
