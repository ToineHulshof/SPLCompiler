equals(x, y) {
    print(x);
    return x == y;
}

x(x) {
    var y = x.hd;
    return y + 1;
}

main() {
    // Bool x = 'a';
    // print(x([3, 4]));
    // print(equals([10], [x]));
    // print("a");
    print(equals([3, 2, 1], [3, 2, 1]));
}

// Functions overloading checken en goede labels fixen
// Errors verbeteren -> in expression dit in funcall dit (mss in state monad, maar wss via function arg)
// Runtime errors
