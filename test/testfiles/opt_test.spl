div(x, y) {
    if (y == 0) {
        
    }
    return x / y;
}

fac(n) {
    if (n <= 1) {
        return n;
    }
    return n * fac(n - 1);
}

main() {
    div(8, 0);
    // var x = 0;
    // while (x < 20) {
    //     print(fac(x));
    //     x = x + 1;
    // }
}