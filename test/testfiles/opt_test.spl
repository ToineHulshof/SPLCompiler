fac(n) {
    if (n <= 1) {
        return n;
    }
    return n * fac(n - 1);
}

main() {
    // var x = 0;
    // while (x < 20) {
    //     print(fac(x));
    //     x = x + 1;
    // }
    var x = 20;
    while (x >= 0) {
        print(1000 / x);
        x = x - 1;
    }
}
