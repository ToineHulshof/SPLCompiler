// equals(x, y) {
//     print(x);
//     return x == y;
// }

// x(x) {
//     var y = x.hd;
//     return y + 1;
// }

f(a, b, c, d) {
    var e = b == c;
    print(d);
    return a;
}

id(x) {
    // print(x);
    return x;
}

main() {
    // Bool x = 'a';
    // print(x([3, 4]));
    // print(equals([10], [x]));
    // print("a");
    // print(f([True], 2, 2, False));
    print(id([2,3]));
    // print(equals([3, 2, 1], [3, 2, 1]));
}

// Functions overloading checken en goede labels fixen
// Errors verbeteren -> in expression dit in funcall dit (mss in state monad, maar wss via function arg)
// Runtime errors
