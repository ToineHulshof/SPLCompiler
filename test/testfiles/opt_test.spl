equals(x, y) {
    print(x);
    return x == y;
}

x(x) {
    var y = x.hd;
    return y + 1;
}

main() {
    // Bool x = 'a';
    // print(x([3, 4]));
    // print(equals([10], [x]));
    // print(hoi);
    print(equals([3], [1]));
}

// substitutions applyen op annotated AST
// Errors verbeteren -> in expression dit in funcall dit
// Runtime errors
// Functions overloading checken en goede labels fixen