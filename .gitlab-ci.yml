image: haskell:8.6.5

variables:
  STACK_ROOT: "${CI_PROJECT_DIR}/.stack-root"

cache:
  paths:
    - .stack-work/
    - .stack-root/

test:
  script:
    - stack --system-ghc build
    # - stack test
    # - ghc --version
    # - stack test
